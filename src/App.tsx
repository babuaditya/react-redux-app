import {BrowserRouter, Routes, Route} from 'react-router-dom'
 /* PLOP_INJECT_IMPORT */
const App=()=>{
  return(
    <BrowserRouter>
    <Routes>
    {/* PLOP_INJECT_ROUTE */}
    </Routes>
    </BrowserRouter>
  );

}
export default App
